# 启用 256 色支持（让颜色更丰富）
set -g default-terminal "screen-256color"
set -sg escape-time 10          # 减少前缀键响应延迟

# ===== 快捷键优化 =====
# 将默认前缀键从 Ctrl-b 改为 Ctrl-a（可选，按需修改）
# set -g prefix C-a
# unbind C-b
# bind C-a send-prefix

# 允许鼠标操作（点击切换窗格、窗口，滚动等）
set -g mouse on

# 重新加载配置无需重启 tmux
bind r source-file ～/.tmux.conf \; display "配置已重载！"

# ===== 状态栏（Status Bar）设置 =====
set -g status-interval 1               # 每秒刷新状态栏
set -g status-justify left             # 窗口列表左对齐
set -g status-position bottom          # 状态栏在底部（默认就是 bottom）
set -g status-style bg=black,fg=white  # 状态栏配色
set -g status-left '#(pwd)'            # 状态栏显示路径

# 状态栏样式
set -g status-style bg=colour234,fg=colour248  # 深灰背景，浅灰文字

# 左侧：会话名 + 当前目录（关键！）
set -g status-left-length 100
set -g status-left '#[fg=colour15,bg=colour27,bold] #S #[fg=colour27,bg=colour234,nobold] #[fg=colour250,bg=colour234]📍 #P:#(pwd) '

# 右侧：时间 + 日期
set -g status-right '#[fg=colour234,bg=colour240,nobold] #[fg=colour15,bg=colour240] %Y-%m-%d  %H:%M #[fg=colour240,bg=colour234,nobold]'

# 窗口状态（中间部分）
setw -g window-status-format '#[fg=colour248,bg=colour234] #I:#W '
setw -g window-status-separator ' '
setw -g window-status-current-format '#[fg=colour15,bg=colour31,bold] #I:#W #[fg=colour31,bg=colour234,nobold]'

# ===== 其他实用设置 =====
# 新窗口从 1 开始编号（而不是 0）
setw -g base-index 1
setw -g pane-base-index 1

# 重新编号窗口（关闭窗口后自动整理）
set -g renumber-windows on

# 允许通过 bash/zsh 的 $TMUX_PANE 获取窗格 ID
set -g set-titles on
set -g set-titles-string '#T'


